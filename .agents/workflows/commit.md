---
description: stagingされた変更に基づき、英語でConventional Commits形式のコミットメッセージを生成する
---

# commit

## 概要
- `git diff --cached` でステージングされた変更を取得し、Conventional Commitsに準拠したコミットメッセージ（英語）を生成します。
- ステージングされた変更がない場合は、メッセージを生成せず、ステージング（`git add`）を行うようユーザーに促します。

## 手順
1. **差分の取得**: `git diff --cached` を実行してステージングされた内容を確認します。
2. **フォーマット**:
   - `<type>(<scope>): <description>` の形式で1行目に要約を記述（英語）。
   - 必要に応じて、詳細な変更理由（Why）をボディ部分に記述。
3. **コミットの実行または提示**: 生成したメッセージをチャットに提示し、ユーザーの確認を待つか、またはコミットコマンドを提案します。
